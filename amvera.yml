meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands:
    - npm install --include=dev
    - npm run build

run:
  scriptName: dist/server.cjs
  persistencePath: /data
  containerPort: 3000
